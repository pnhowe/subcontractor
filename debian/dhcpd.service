[Unit]
Description=Subcontractor DHCPd service
After=network.target

[Service]
Type=simple
ExecStart=/usr/bin/dhcpd -p /var/run/dhcpd.pid -c /etc/subcontractor.conf -i foreground
DefaultTimeoutStopSec=120

[Install]
WantedBy=multi-user.target
